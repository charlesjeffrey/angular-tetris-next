import { ConvertDegreesToDirectionPipe } from './convert-degrees-to-direction.pipe';

describe('ConvertDegreesToDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDegreesToDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
